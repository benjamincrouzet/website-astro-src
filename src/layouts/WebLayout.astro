---
import WebHeader from '../components/Web-Header.astro';
import WebFooter from '../components/Web-Footer.astro';
import '../styles/styles.scss';
   
   interface WebLayoutProps {
        langage?: string;
        title?: string;
        card?: string;
    }

    const {
        langage="fr", 
        title = "Hisotire",
        card = "card",
        } = Astro.props;
---
    <!DOCTYPE html>
    <html lang={langage}>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" href="/Assets/histoire.ico">    
        <title>{title}</title>
    </head>
    <body>
    
        <WebHeader header={title} />
       
        <slot />

       <WebFooter footer="le créateur de ce site" />

    </body>
    </html>